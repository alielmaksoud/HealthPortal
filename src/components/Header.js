import React, { Component } from 'react';

import Header from './Header';

export default class App extends Component {
  render() {
    return (
      <nav className="page-header navbar navbar-inverse">
        <div className="navbar-header navbar-brand">
       		HealthPortal
        </div>
      </nav>
    );
  }
}
